Looking for correlations between bar slope and other factors

The program "bar.py" finds the bar slopes for each bar measured in the dataset. It writes the theta values to a csv. The program "Analysis.py" looks for correlations with other factors at the reach scale. It will also produce Figure 4c.
